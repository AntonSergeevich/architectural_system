# 08. Цены и приём оплаты

---

## Откуда взялись цены

Часть назвала Дарья, часть поставлена по верхней планке рынка Красноярска —
КП студии KRUG (2026) — как ориентир до её решения.

| Позиция | Откуда цена |
|---|---|
| Проектирование | Верхняя планка рынка: 3 800 ₽/м² за полный проект |
| Авторский надзор | Дарья: 50 000 ₽/мес |
| Комплектация | Дарья: 20 000 ₽/мес *(«надо посмотреть, кто за сколько продаёт»)* |
| Консультация | Дарья: 8 000 ₽ за 2 часа, далее +1 000 ₽/час |
| Помещение до 20 м² | Дарья: 80 000 ₽ фиксировано за любой пакет |
| Выбор квартиры | Предложение: 10 000 ₽ за 2 часа, далее +1 000 ₽/час |

## Как 3 800 ₽/м² разложены по модулям

Каталог модульный, поэтому единую ставку пришлось разложить. Разложена она
так, чтобы **сумма модулей «за м²» в пресете «Полный проект» давала ровно
3 800 ₽/м²**:

| Код | Модуль | Единица | Цена |
|---|---|---|---|
| A1 | Обмер и обмерный план | фикс | 8 000 ₽ |
| A2 | Бриф и техзадание | фикс | 6 000 ₽ |
| A3 | Планировочные решения | м² | 700 ₽ |
| A4 | Концепция: эскизы и коллажи | м² | 600 ₽ |
| A5 | Реальные материалы и артикулы | м² | 700 ₽ |
| A6a | Ручные эскизы | м² | 0 ₽ (входит в A4) |
| A6b | Нейро-визуализация | м² | 650 ₽ *(скрыта)* |
| A6c | Фотореалистичное 3D | м² | 900 ₽ |
| A7 | Комплект чертежей, 17 листов | м² | 900 ₽ |
| A7p | Частичный комплект | — | индивидуально |
| A8 | Сборка альбома и закрытие | фикс | 6 000 ₽ |
| Б1 | Авторский надзор, город | мес | 50 000 ₽ |
| Б1в | Разовый выезд | выезд | 15 000 ₽ |
| Б2 | Сопровождение удалённо | мес | 50 000 ₽ |
| Б3 | Комплектация | мес | 20 000 ₽ |
| В1 | Консультация | 2 ч + час | 8 000 ₽ / +1 000 ₽ |
| В4 | Консультация по выбору квартиры | 2 ч + час | 10 000 ₽ / +1 000 ₽ |
| В3 | Декоратор и съёмка | — | индивидуально |

Проверка: 700 + 600 + 700 + 900 + 900 = **3 800 ₽/м²**. Плюс 20 000 ₽
фиксированных за обмер, бриф и альбом.

**Почему обмер платный отдельно.** Это 2–3 часа работы, и делать её
бесплатно неправильно — см. [03-процесс.md](03-процесс.md). Договор
подписывается до обмера, цена пересчитывается по фактической площади.

---

## Три правила расчёта поверх прайса

Живут в «Кабинет → Цены → Правила расчёта», правятся Дарьей.

### Фикс для помещений до 20 м²

Любой пакет услуг для помещения такой площади стоит **80 000 ₽**. Причина
не в скидке и не в наценке: на маленьком метраже расчёт за квадрат даёт
сумму, за которую работать нельзя, а включений там ровно столько же,
сколько в комнате втрое больше. Порог и цена меняются в кабинете, правило
целиком выключается галочкой.

Надзор и комплектация к фиксу прибавляются сверху — они считаются
от срока стройки, а не от площади.

### Коэффициент сложности — сделан, но выключен

Дарья сомневалась, нужен ли он: по факту она всегда работала по одной цене,
и вензеля, и минимализм требуют одинакового включения. Правило **выключено**
до сих пор: вопросы про сложность на сайте не показываются, цена одна для
всех. Включается одной галочкой в разделе «Цены», ничего дописывать
не придётся.

Коэффициент складывается из двух половин разной природы.

**Характер интерьера** — выбирается одно, коэффициент множитель:
спокойный и лаконичный (1.00), с характером (1.15), авторский (1.30).

**Обстоятельства объекта** — отмечается сколько есть, коэффициент надбавка.
Формулировки Дарьи, это ровно те случаи, из-за которых две одинаковые
по площади квартиры занимают разное время:

| Обстоятельство | Надбавка | Почему |
|---|---|---|
| Ремонт уже начат без проекта | +0.15 | Приходится разбираться в готовых коммуникациях и подстраивать проект под сделанное |
| Материалы частично закуплены | +0.10 | Проект собирается вокруг купленного, свобода решений сужается |
| Старый фонд | +0.15 | Кривые стены, ветхие коммуникации, охранный статус — каждый узел решается отдельно |

Половины **складываются**, а не перемножаются: «характер 1.15, старый фонд
плюс 0.15, начатый ремонт плюс 0.15, итого 1.45» — это можно сказать вслух
на встрече. Перемноженные проценты не объясняются никак.

Обстоятельства нельзя было сделать выбором одного из списка: начатый ремонт
и старый фонд случаются вместе, и заставлять выбирать между ними значит
занизить цену ровно там, где работы больше всего.

### Надзор считается за весь срок стройки

Дарья хочет продавать надзор как неотъемлемую часть проекта — значит
и считать его надо сразу, а не «плюс когда-нибудь». Срок берётся
из площади: **около года на 100 м²** — её средний показатель от демонтажа
до картины на стене. Заказчик может поправить число месяцев.

Итог показывается так, как она предложила: **проектирование отдельной
строкой, надзор с комплектацией отдельной**. Плюс общий итог — по её же
логике: «50 000 в месяц» выглядят небольшой суммой ровно до тех пор, пока
их не умножили на двенадцать. Узнать это на восьмом месяце стройки хуже,
чем на сайте. Общий итог тоже отключается галочкой.

**Нейро-визуализация (A6b) скрыта.** Цена есть, на сайте не показывается:
Дарья сказала, что навык надо подтянуть. Включается галочкой в кабинете.

---

## Как Дарья меняет цены

**Кабинет → Цены.** Таблица со всеми модулями: цена и галочка «показывать
на сайте». Изменение применяется сразу — на сайте, в конструкторе, в новых
КП.

Что происходит при этом:

- **Уже выставленные КП не меняются.** Цена копируется в позицию КП
  в момент выставления, а не берётся из каталога при открытии. Иначе
  заказчик открыл бы вчерашнее предложение и увидел другую сумму.
- **Пишется история.** Каждое изменение попадает в журнал цен с датой
  и комментарием. Через год не придётся гадать, почему в старом КП стояла
  другая цифра.
- **Обновление кода цены не трогает.** Команда `seed_catalog` обновляет
  описания модулей, но цены оставляет как есть.

Коэффициенты сложности и состав пресетов правятся в админке — они меняются
заметно реже.

---

## Договоры

Три шаблона: проектирование, авторский надзор, комплектация. Все три
редактируются в **Кабинет → Договоры**:

- текст каждого пункта;
- расшифровка **на человеческом языке** — та самая, которую коллеги
  проговаривают маркером, сидя рядом с заказчиком;
- галочка «важный» — подсветить пункт.

Заказчик открывает договор по ссылке, видит подсветку, раскрывает
расшифровки и отмечает пункты, по которым есть вопрос. Дарья разбирает
только отмеченное, а не весь документ.

Отдельно фиксируется подтверждение «прочитал и понял»: имя, дата, редакция
шаблона. Именно это закрывает классику «а почему вы мне в воскресенье
не ответили» — регламент подтверждён, и подтверждение записано.

---

## Юридические документы и куки

Четыре документа, все редактируются в админке, у каждого своя версия и дата
редакции:

| Документ | Зачем |
|---|---|
| Политика конфиденциальности | Требование 152-ФЗ |
| Согласие на обработку персональных данных | Отдельная галочка в каждой форме |
| Политика в отношении файлов cookie | Что ставится и зачем |
| Публичная оферта | В том числе оговорка: расчёт конструктора — ориентир, а не оферта в части цены |

**Тексты — рабочие заготовки.** Они закрывают требования по смыслу,
но перед публикацией их должен посмотреть юрист: ответственность
за документы на сайте несёт владелец сайта.

### Как устроены согласия

Два разных согласия с разными основаниями, и одной галочкой их закрывать
нельзя:

**Куки.** Баннер с двумя кнопками: «только необходимые» и «принять все».
Выбор пишется в журнал: дата, что выбрал, какую редакцию политики видел,
браузер. Закон требует доказуемости, а доказать можно только записанное.
Аналитика не включается, пока согласия нет. Кука согласия живёт 180 дней.

**Персональные данные.** Отдельная обязательная галочка в форме заявки.
Без неё заявка не отправляется. Факт согласия записывается вместе
с версией документа, который человек в тот момент видел, — иначе оно
ничего не доказывает.

---

## Своя услуга в прайсе

Каталог заведён под то, как Дарья работает сегодня, но работа меняется:
появляется услуга, которой год назад не было. В разделе «Цены» её можно
завести самой — название, блок, единица, цена, срок. Код система выдаёт
сама: он нужен ей (по нему связаны пресеты и старые КП), а человеку —
ни разу.

Форма отдельная, а не внутри общей формы цен: там правятся десятки чисел
разом, и однажды новая услуга завелась бы просто от нажатия «сохранить».

Удаления нет намеренно — есть галочка «На сайте». Услуга может стоять
в выставленных КП и подписанных договорах, и удалять её оттуда задним
числом нельзя.

## Приём оплаты: GetPlatinum

Устроено так: счёт → ссылка → оплата на стороне сервиса → уведомление →
статус счёта. Данные карты через сайт не проходят и нигде не хранятся.

**Решение на сегодня: эквайринг не подключаем.** Комиссия берётся
с каждого счёта, а счетов единицы — перевод по номеру телефона занимает
у заказчика столько же времени, сколько ввод карты. Это не аварийный
режим, а выбранный рабочий.

Что видит заказчик вместо кнопки: раздел «Как оплатить» с реквизитами
перевода. Текст пишет Дарья в настройках сайта — номер для СБП, банк,
назначение платежа; пока поле пустое, показывается её телефон. Внизу
всегда добавляется номер счёта для назначения платежа: без него перевод
приходится опознавать по сумме и времени.

Поступившую оплату Дарья отмечает в кабинете руками. Счёт закрывается
и считается в деньгах проекта наравне с любым другим — на всей остальной
системе отсутствие эквайринга не сказывается никак.

### Что нужно, чтобы включить

Две строки в `.env`: адрес API вида
`https://<аккаунт>.getplatinum.ru/api/public/pay` и API-ключ из их
личного кабинета (Настройки → организация → API-ключ). Отдельного секрета
для подписи у них нет — тот же ключ.

Адрес для уведомлений настраивать у них не нужно: мы передаём его сами
в каждом запросе. Подробнее — в [DEPLOY.md](../DEPLOY.md), раздел 9.

### Как это устроено

Используется метод `init-payment-url`: он отдаёт ссылку на платёжную форму
одним запросом, минуя двухшаговую схему «инициализация заказа →
инициализация платежа». Показывать заказчику список платёжных систем нам
незачем — пусть выбирает на их форме.

Авторизация — `Authorization: Bearer <API-ключ>`. Суммы везде в копейках.
Идентификатор заказа на их стороне — `INV-<номер счёта>`. Позиция чека
идёт с категорией «Консультация»: из их списка (он заточен под инфобизнес)
это единственное, что описывает услуги дизайнера без натяжки.

Ставка НДС — `none`, «не облагается». Для самозанятого это верно; `0`
означает другое — ставку 0 %, и ошибка здесь ведёт к проблемам
с налоговым учётом. Поэтому значение вынесено в настройки, а не зашито
в код.

### Три вещи из их документации, которые определили реализацию

**Коллбэк приходит ровно один раз.** Если ответить не 200, повторной
попытки не будет — платёж останется неучтённым навсегда. Поэтому
обработчик отвечает 200 **всегда**, а «принял» и «зачислил» у него разные
вещи: принимаем всё, зачисляем только проверенное.

**Редирект на successUrl оплату не подтверждает.** Это сказано у них
прямо, и это логично: адрес «спасибо» можно открыть руками. Подтверждает
только коллбэк.

**Подпись считается по вложенной структуре.** Параметры сортируются без
учёта регистра, склеиваются как `ключ;значение;`, объекты сериализуются
в JSON, булевы — в 1/0, `checksum` и `customParams` исключаются, результат —
HMAC-SHA256 в верхнем регистре. Формула покрыта тестом на примере из их же
документации.

Но в сериализации вложенных объектов разойтись с чужой реализацией легко —
лишний пробел, другое экранирование. Молча выбросить уведомление
с несошедшейся подписью значит потерять деньги. Поэтому при расхождении
система не отбрасывает его, а идёт и спрашивает статус платежа напрямую
через их метод `/status`. Хуже несовпавшей подписи только потерянный
платёж.

Сверх этого раз в час по крону работает `sync_payments`: проходит
по счетам, где оплата начиналась, но не завершилась, и добирает те,
по которым коллбэк не дошёл вовсе — упала сеть, перезагружался сервер.

### Что сделано правильно независимо от протокола

- **Подпись проверяется всегда.** Без верной подписи уведомление
  отклоняется. Сравнение — постоянным по времени методом: обычное `==`
  на секретах позволяет подобрать подпись по времени ответа.
- **Повторные уведомления не задваивают платёж.** Провайдер присылает
  уведомление не один раз — это норма, так устроены вебхуки. Защита
  на уровне базы: уникальность пары «провайдер + идентификатор платежа»,
  а не надежда на то, что сеть не подведёт.
- **Возврат из платёжной формы не считается оплатой.** Оплату подтверждает
  только уведомление от сервиса. Иначе достаточно было бы открыть адрес
  «спасибо» руками.
- **Частичная оплата обрабатывается.** Счёт остаётся открытым на остаток.
- **Неудачный платёж не понижает статус счёта.** Выставленный счёт
  не превращается обратно в черновик.
- **Уведомления выведены из-под лимита запросов в nginx.** Иначе часть
  их отбросится, и оплаченные счета останутся неоплаченными в системе.
